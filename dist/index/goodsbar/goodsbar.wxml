<!-- 主页商品栏组件 -->
<!-- 头部 -->
<view class="goodsbarHeader">
  <view class="goodsbarTitle">{{Title}}</view>
  <view class="goodsbarSecondTitle" hidden="{{SecondTitleSign}}">{{SecondTitle}}</view>
  <view class="goodsbarTime">
    <van-count-down use-slot millisecond="{{millisecond}}" time="{{ time }}" bind:change="onChange" auto-start="{{autoStart}}">
      剩余
      <text class="item">{{ timeData.hours }}</text>
      <text>:</text>
      <text class="item">{{ timeData.minutes }}</text>
      <text>:</text>
      <text class="item">{{ timeData.seconds }}</text>
      <text wx:if="{{millisecond}}">:</text>
      <text class="item" wx:if="{{millisecond}}">{{ timeData.milliseconds }}</text>
    </van-count-down>
  </view>
  <view class="more">{{more}}</view>
</view>
<!-- 四栏商品 -->
<view class="fourGoodsbar">
  <view class="imgRow">
    <view wx:for="{{imgs}}" wx:key="index">
      <image class="goodImg" src="{{item}}" mode="scaleToFill" lazy-load="true"></image>
      <image class="goodDiscountImg" src="{{disCountImg}}" mode="scaleToFill" lazy-load="true"></image>
      <view class="fourDiscountText">{{fourDiscountText[index]}}</view>
    </view>
  </view>
  <view class="TitleRow">
  <view class="TitleCol" wx:for="{{TitleText}}" wx:key="index">{{TitleText[index]}}
  </view>
  </view>
  <view class="MinTitleRow"></view>
  <view class="priceRow"></view>
</view>